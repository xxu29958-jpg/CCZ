# Add Event Op

Use this workflow when adding a native event operation.

1. Add the op to the native event model.
2. Add validation for required fields and references.
3. Add gameplay execution behavior.
4. Add presentation event output if needed.
5. Add a sample content snippet.
6. Add tests for valid and invalid content.
7. Do not add legacy opcode assumptions unless backed by a real sample.

