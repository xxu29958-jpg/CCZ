# Release Runbook

Release flow is not configured yet.

Future release checks:

- Native content pack validator passes.
- Core golden tests pass.
- Android release build completes.
- Debug-only tooling is absent from release UX.
- Version fields are updated intentionally.

