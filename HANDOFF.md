# Handoff

> 状态：引擎实现阶段收尾 + **P2 渲染半推进（战场渲染 + tap→Move + tap→Attack + 事件驱动表现层 + 多技能选择 + R 剧本过场演出 + 战斗胜负终局 + 行动经济 + 敌方 AI（战斗可玩闭环）落地）+ 内容驱动战役驱动层（战斗 + intro 过场：content 包→`CampaignAssembler` 战斗装配 / `RScript` 过场装载→`:app` 组合根运行，`DemoBattle`/`DemoScenario` 去硬编码）**。引擎侧（P1 内容 loader / Save·Replay 信封 + on-disk codec + 命令完整性 / 事件脚本 JSON 解码 / 内容→战斗装配 / R 剧本校验 + ScenarioRunner / scenario 进存档 + 回放 / spawn·move·setHp 落点 fail-closed / `:save-io` 原子写 / fail-closed gap 团队扫荡 8 片）+ 表现层 `:app`（Compose 壳 + 战场渲染 + tap→Move/Attack + 事件驱动徽章 + 多技能 + 过场演出 + 胜负终局 + 行动经济 + 敌方 AI）+ 小票夹工程移植批（机器门 #41–#48 + 工程文档 #60/#61）均**已合并** main，CI 全绿（jvm-gate + android-gate 两 lane），本批补上 `CampaignReplayDriver` replay 接线、`manifest.entry` 校验、脚本地图上界校验与 `pre` 部署顺序校验。逐 PR 与分片明细见下方「已合并 PR」列表与各分片记录、以及 PR 描述 / git log——不在本状态行重述以免 run-on（移植 backlog #4）。

## 当前真实状态

- 当前顶部为 `CampaignAssembler` `pre` 部署顺序校验（本批），基于 `origin/main` 的 mid-trigger tick（#66）继续推进。CI（`.github/workflows/ci.yml`）每 push/PR 跑两 lane：**jvm-gate**（`:game-core`/`:native-content`/`:save-io` test+selfTest+detekt+count + 三根门 `assertModuleDependencyDirection`/`verifyTextEncoding`/`assertStableDependencyVersions`）+ **android-gate**（`:app` detekt + **单测真跑（`testGrayDebugUnitTest`）** + lint/count/release 装配，装 `android-36`+build-tools）。JVM test=261、`:app` test=54（独立 baseline）。通用规则 `GENERAL_ENGINEERING_RULES.md` v0.2.1。已合并 PR：#3 P2 合法性、#4 P3a 胜负、#5 P3b 触发器、#6 P1 loader、#7 Save 信封、#8 事件脚本 JSON 解码、#9 内容→战斗装配 reserves、#10 R 剧本引用校验、#11 R 剧本 ScenarioRunner、#12 ScenarioRunner Choice 续跑、#13 Save on-disk codec、#14 Save 命令完整性、#15 scenario 进存档、#16 scenario 回放执行、团队 review 跟进 cleanup PR、#18 SpawnUnit 目标格 fail-closed 校验、#20 MoveUnit 落点 fail-closed 校验、#21 `:save-io` 存档原子写、#22-#29 fail-closed gap 团队扫荡 8 片（见下「团队 fail-closed 扫荡」）、#31 `:app` Compose 壳（P2 渲染半起步 + CI android-gate lane）、#32 `:app` 战场渲染 + tap→Move（见下「app 战场渲染」）、#33 `:app` tap→Attack 目标选择 UX + 射程高亮（见下「app 攻击目标选择」）、#35 `:app` 事件驱动表现层（`Event` 流→飘字徽章，见下「app 事件驱动表现层」）、#37 单位技能 loadout 权威 + `:app` 多技能选择 UX（`Gameplay.legalSkills` + loadout 校验，见下「多技能选择 + loadout 权威」）、#39 权威 actor 前置 + 表现层占用查询单一化（高内聚 refactor，见下「内聚 refactor」）、#41 小票夹规则金矿移植（高内聚低耦合总纲 + 注释纪律 + tripwire，规则 v0.2.0）、#42 `RejectReason→phrase` 层 + detekt 门细化、#44 依赖方向门 `assertModuleDependencyDirection`（高内聚低耦合总纲升 `[machine-gated]`）、#46 文本编码门 `verifyTextEncoding`（UTF-8/.ps1 BOM/无乱码升 `[machine-gated]`）、#48 依赖版本审计门 `assertStableDependencyVersions`（禁 alpha/beta/rc/snapshot 升 `[machine-gated]`，见下「小票夹金矿移植」）、#50 `:app` R 剧本过场表现层 `ScenarioReducer`/`ScenarioScreen`（事件驱动 Scenario 演出，见下「app 过场表现层」）、#52 战斗胜负判定上表现层 `Gameplay.outcome` 终局门（只读查询 + `:app` 终局冻结输入，见下「app 战斗终局」）、#54 行动经济（`Command.Wait` + `moved`/`acted` 每单位每回合一移一动、move-then-attack，敌方 AI 前置，见下「行动经济」）、#56 敌方 AI（确定性侵略式 `EnemyAi` 纯规划器 + `:app` 自动敌方回合，战斗成可玩闭环，见下「敌方 AI」）、#58 敌方 AI 走位精修、#63-#66 content-driven battle / JSON resource / mid-trigger tick。
- **fail-closed gap 团队扫荡（#22-#29，本轮）**：团队模式（多 agent 发现→对抗审）扫出 13 个 self-contained fail-open gap，收敛 8 片，每片单独 PR + 全量门 + 三镜头对抗审（correctness / scope-determinism / coverage）0 P1。S1 事件脚本值域（`HpBelow.pct∈0..100` / `SurviveTurns.turns≥1` / 落点四处非负，`ContentEventValidator`）；S2 内容表引用补全（`ItemDef.equipClass`→class、`ClassCombat.terrainAffinity` key→terrain，`ContentValidator`）；S3 S 剧本内嵌 scenario op（`BattleOp.Script` 拆出：Portrait 走 unit 检查、Branch/Choice 标 unsupported）；S5 S 剧本 `BattleTrigger.id` 唯一（once 触发按 id 键 `firedTriggers`，dup 会互相压制）；S4 `events.sScripts`/`rScripts` 的 id 跨表唯一 + gitignore `**/.kotlin/`；S6 数值域（`TerrainDef.moveCost≥1` 镜像 `MapTile`、`SkillDef.range min∈0..max` 镜像 `RangeSpec`，`min==0` 合法）；S8 `BattleOps.setHp` 复活落点 fail-closed（仅 dead→alive 边复用 `blockedTile`/OCCUPIED，新 `Event.HpSetRejected`，与 spawn/move 对称——SetHp 是最后一处未守落点边）；S7 save 加载双轴（`SaveMappers` decode 校验 units map key==`identity.id` 抛 `SaveDecodeException`；`SaveLoader.load` 加 scenarioIntegrity + `SaveRejection.CORRUPT_SCENARIO`）。全程 `Resolver`/RNG/伤害公式/golden/`RULES_VERSION` 零碰。
- **game-core（P2 引擎半）**：`Gameplay.submit` 合法性闸门 + `CommandValidator` + `BattleMap`/`MoveReachability` + `BattleContext` + `Skill.range`。回合归属按侧；Move 自身格 = no-op。
- **game-core（P3 事件引擎）**：`WinLose`（胜负，OR/lose 优先/sticky）+ `TriggerRunner`（`tick`=触发器→settle，`applyPre/Post`）+ `TriggerConditions`（6 条，整数 HP）+ `BattleOps`（9 ops，纯无 RNG）+ `ScriptContext.reserves`。`once` 经 `BattleProgress.firedTriggers`。`outcome`/`vars`/`firedTriggers` 收进 `BattleProgress`。
- **game-core（事件 placement op 落点 fail-closed，spawn + move 对称）**：`BattleOps.spawn` 与 `move` 共享 `blockedTile(state, at, excludeUnit, map)` 守卫 —— 越界 / 不可通行 / 占用 → no-op + `Event.SpawnRejected`/`MoveRejected(unit, reason)`，**不伪造 state**（此前两 op 均零落点校验，可把两个存活单位堆一格、放越界/墙格）。**占用恒查**（`occupancyOf(state, exclude = op.unit)`，目标自格不自阻镜像 `CommandValidator.checkMove`，守"一格至多一活单位"单源不变式，不需 map 即生效）；**边界/可通行**经 `ScriptContext` 可选 `BattleMap`（`map == null` 仅放宽这两项，占用不放宽）。`map` **尚未接进生产 spawn/move 路径**（无 `:app`/战役驱动层，`BattleAssembler.scriptContext` 不传 map），故 `OUT_OF_BOUNDS`/`IMPASSABLE` 生产 dormant —— `PlacementReject` KDoc 诚实标注（不给未接线能力写现在时断言）。spawn 缺模板 surface `NO_TEMPLATE`（spawn 专有，`MoveRejected` 不携带）。决策记于 **ADR-0004**（脚本 placement op 尊重占用/地形，无 teleport 逃逸口；未来如需 cutscene teleport 须新增显式 op 变体另开 ADR）。`Resolver`/RNG/golden/`RULES_VERSION` 零碰；两 op 仍 `(state, op, ctx)` 纯确定。
- **game-core（Save/Replay 信封 + on-disk codec）**：`com.ccz.core.save` —— `SaveEnvelope`/`SaveVersions`（六版本轴）+ `SaveLoader`（fail-closed 拒未来 save schema / 规则漂移，折叠已接受命令确定性回放）+ `SaveCodec`（`SaveEnvelope`↔JSON：`@Serializable` `SaveDto` 镜像全树 + `SaveMappers` 双向映射 + strict Json，encodeDefaults 显式存档；坏 JSON / 未知 key / 未知命令 kind / 未知 faction·outcome → `SaveDecodeException`，fail-closed）。`SaveLoader.load` 两闸：版本（FUTURE_SCHEMA / RULES_MISMATCH，`check`）→ 命令完整性（`commandIntegrity` replay 前校验命令引用 unit/skill 对初始 roster / skill 表可解析，缺失 → `CORRUPT_COMMAND` 优雅拒而非 replay fold 抛异常）；codec（`SaveCodec.decode`，**独立解码入口**）先校验 JSON shape。域类型零序列化注解（DTO 隔离，守规则）。`SaveEnvelope.scenarios`（`ScenarioReplay` = scriptId + choices）= 与战斗 command 并列的第二回放轴，`SUPPORTED_SAVE_SCHEMA_VERSION` 1→2；v1 存档缺 scenarios 字段 → decode 为空（forward-compatible）。scenario 回放经 `ScenarioReplayer`（过场回放对位件：对 `SaveEnvelope.scenarios` 跑 `ScenarioRunner`，scriptId 缺失→`UNKNOWN_SCRIPT`、choices 截断/脚本环→`INCOMPLETE_REPLAY`，fail-closed 不漏 partial）。存档落盘的原子写在独立 `:save-io` 模块（见下）。
- **save-io（存档原子写 IO 边界）**：`com.ccz.saveio` 纯 JVM 模块（依赖 `:game-core`，serialization 经其 runtime 传递；game-core 仍零文件系统，IO 隔离于此）—— `SaveFileStore.save` 写**同目录**临时文件 + `Files.move(ATOMIC_MOVE)` 覆盖目标，`try/finally` 保证任何非成功退出（含 unchecked/Error）都清理临时文件（读者/崩溃永不见半写、不留半文件/残留 temp）；`load` 读 + `SaveCodec.decode`，缺文件 → `SaveIoException`、坏内容 → `SaveDecodeException`（区分 IO 与 decode）。原子性 = rename 可见性，**非** crash durability（fsync 显式注为越界未来加固，不过度声称）。CI / LOCAL_DEV / CI.md / RULES gate 块均加 `:save-io` 四任务，count baseline `testRoots` 含 save-io。`Resolver`/RNG/golden/`RULES_VERSION` 零碰。
- **native-content（P1 JSON loader，仅 tables）**：`com.ccz.contentpack.json` —— `ContentJsonLoader.load(json)`（strict、snake_case、`ignoreUnknownKeys=false`）+ `@Serializable` DTO + 映射器 + 枚举白名单 `Decoders`（fail-closed）。两层 fail-closed：解码 shape → `ContentValidator` 查引用/版本。`kotlinx-serialization-json:1.7.3` 现入 `:native-content` 与 `:game-core`（后者用于 Save codec，2026-06 经用户 sanction）；两处的 `game-core` **域类型**均零序列化注解（DTO 层隔离 JSON，守规则）。
- **native-content（事件脚本 JSON 解码，P1 续）**：`EventDto`/`EventMappers` —— R/S 脚本的多态 op 层级（`ScenarioOp` 11 / `TriggerCondition` 6 / `BattleOp` 9 / `WinLoseCondition` 6）经 kotlinx sealed + class-discriminator `type` 解码；discriminator 值（显式 snake_case `@SerialName`）**即 op 字符串白名单**——未知 op 无注册子类 → `SerializationException` → `ContentDecodeException`（fail-closed）。含嵌套多态（`BattleOp.Script` 包 `ScenarioOp`）。embedded faction 走 `decodeFaction` 白名单（fail-closed，直接抛 `ContentDecodeException`）；reference 整性仍在下游 `ContentEventValidator`。`EventTables` 现可从 JSON 装载（`ContentDto.events` 默认空、向后兼容）。每 op/condition/win-lose 变体过 decode→map 结构等值断言（防字段错位）。
- **native-content（内容→战斗装配，reserves）**：`com.ccz.contentpack.assembly.BattleAssembler` —— 把已验证 `UnitDef` 映射成 off-map `Combatant` 模板（满血入场、`pos=(-1,-1)` 占位由 `SpawnUnit op.at` 覆盖、`stats`/`rates` 直传、`level`/`loadout`/`assets` 不入战斗权威——面板已折算），打包 `ScriptContext.reserves`（按 unit id 索引），让 `SpawnUnit` op 真正取到 reserve（此前 reserves 恒空 = spawn 恒 no-op）。端到端经 `TriggerRunner.applyPre` 公开入口验证（`BattleOps` internal）。装配层单向依赖 game-core，game-core 零改动；selftest 加装配冒烟。
- **native-content（R 剧本引用校验，fail-closed）**：`ContentEventValidator` 扩展覆盖 `rScripts` —— `Branch.target` / `Choice.option.goto`（非 null）须指向本剧本内存在的 `Label`（两遍扫描，支持前向引用）；`Portrait.unit` 须为已知 unit 或 `events.portrait_subjects` 非战斗主体；同剧本 `Label` 名唯一。穷尽 when（无 else，sealed 编译期兜底）；未知引用 → `ValidationIssue` 不崩。scene/bgm 资源与 scenario 动态变量键无 id 表，故不在引用校验范围。补此前 R 剧本零校验的 fail-open 缺口；为 scenario runner 铺路。
- **game-core（R 剧本 ScenarioRunner，确定性执行 + Choice 续跑）**：`com.ccz.core.event.ScenarioRunner.run(script, vars, choices)` —— PC 走 `RScript.ops`，控制流（`Label`/`SetVar`/`Branch`）消化为 vars 演化 + 跳转，演出 op（Dialogue/Portrait/Wait/SceneTransition/PlayBgm/Fade）按序进 `Playback.events` 供表现层。`Choice` 消费 `choices` 下一个选择索引（可回放输入轴）→ 应用 `option.setVars` + jump 到 `goto`；choices 耗尽/索引越界（含负）→ 停 `pausedAt` fail-safe。`(script, vars, choices)` 确定输出。纯无 RNG，整数 vars（未设读 0，与 `BattleProgress.vars` 一致）；步数预算（ops×100）防 branch 环（超限 `haltedOnBudget` fail-safe）；未解析 target 视为不跳转而非崩。穷尽 when（sealed 编译期兜底）。`Resolver`/RNG/golden 零碰；`branchTaken`/`jump`/`chosenOption` helper 控圈复杂度。
- **app（`:app` Compose 壳，P2 渲染半起步，#31）**：`com.ccz.app` Android 模块（AGP 9.2.0 + Jetpack Compose）—— **表现层零战斗权威** 的壳起步。gray flavor（灰度分发渠道，`[machine-gated]` Android :app gate 钉此 variant）；release 现产**未签名**不 minify APK（R8/签名才是未来门）。`compileSdk/targetSdk=36`、`minSdk=26`（本机仅装 `android-36`）。AGP 9 移除 kotlin-android 插件 → `:app` 走 AGP 内置 Kotlin 编译器 2.3.10，Compose 编译器插件 pin 同版（JVM 模块仍 2.2.21，统一升另开 slice）。CI `android-gate` lane + 本地 Full Gate，已翻 `[machine-gated]`（CCZ_ENGINE_RULES §Android App Gates）。
- **app 战场渲染 + tap→Move（P2 渲染半，#32，本片）**：`com.ccz.app.battle` —— Compose `BattleBoard`（7×6 网格：地形底色 + 单位标记按 faction 上色 + 选中环 + game-core 报的可达格高亮 + 事件日志）+ `BattleScreen`（HUD 回合/选中/End Turn + 板 + 日志）+ tap 输入循环（点己方活动单位选中 → 点高亮可达格落 `Command.Move`、End Turn 换边），**全部经 `Gameplay.submit` 下命令，表现层持零战斗真相**——不算伤害 / 不挪改单位 / 不消费 RNG / 不裁胜负，只把 `Gameplay` 返回的 `BattleState`/`events` 整体换上去渲染（镜像 §Seven Hard Rules #1/#2）。核心件：**`BattleReducer`**（纯 reducer，零 Android 依赖、JVM 可测，tap 路由 + `submit` + 日志；stale 高亮 → `submit` 拒 → fail-closed 不伪造移动）；**`DemoBattle`**（硬编码表现层种子，临时填充直到内容驱动层落地，**非第二套真相**——只作初始输入交给 core）。game-core 侧仅新增**只读查询** `Gameplay.legalDestinations(state, unit, ctx)`：暴露单位可落点集供 UI 高亮，空间规则仍归核（复用 internal `MoveReachability`/`occupancyOf`），纯无 RNG、不改 state；`submit` 仍是唯一落子权威（`GameplayQueryTest` 钉「query 报的格 ⟺ submit 接受的格」）。**门补强**：新增 `:app:testGrayDebugUnitTest` 进 CI `android-gate` + 本地 Full Gate + 规则——此前 `:app` 单测只被计数从不执行，本片把表现层 reducer 逻辑接进**真跑门**（计数门钉测试不被偷删，执行门钉断言真过）；test baseline 根 201→206、`:app` 1→8。三镜头对抗审（correctness/architecture-determinism/coverage）0 P1/P2，前两镜头零发现。`Resolver`/RNG/`Formula`/`RULES_VERSION`/golden 零碰。
- **app 攻击目标选择 + 射程高亮（P2 渲染半，#33，本片）**：`com.ccz.app.battle` —— `BattleReducer` 加攻击路由（选中己方活动单位后 tap 命中其 `targets` 内敌格 → 经 `Gameplay.submit` 下 `Command.Attack`；**stale targets → submit 拒 → fail-closed 不伪造伤害**）+ `BattleBoard` 可攻目标红环/红底高亮 + `BattleUiState.targets`。表现层仍持**零战斗权威**——不算伤害 / 不判射程 / 不裁合法性 / 不消费 RNG，只渲染 game-core 报的可攻目标并把权威返回的 `events`（Damaged/Missed/Died）换上去。game-core 侧仅新增**只读查询** `Gameplay.legalTargets(state, attackerId, skillId, context)`：暴露攻击者可合法 `Command.Attack` 的单位 id 集供 UI 高亮，targeting 规则仍归核（镜像 `CommandValidator.checkAttack` 接受条件：活动侧归属 / 攻击者存活 / skill 已知 / 排除自身与同侧 / 目标存活 / `RangeSpec` 射程），纯无 RNG、不改 state；`submit` 仍唯一落子权威（`GameplayQueryTest` 钉「query 报的目标 ⟺ submit 接受的目标」）。`basicAttackSkill` 经 `BattleReducer` 构造传入（单基础攻击，多技能 picker 留后片），`DemoBattle.BASIC_ATTACK` 提供 id、Zhang 移至 (4,2) 邻接敌方弓手使首回合即可演示攻击。**门补强**：`GameplayQueryTest` +5（JVM 206→211）、`BattleReducerTest` +4（:app 8→12）。三镜头对抗审（correctness/architecture-determinism/coverage-failclosed）**0 发现**。`Resolver`/RNG/`Formula`/`RULES_VERSION`/golden 零碰。
- **app 事件驱动表现层（P2 渲染半，#35，本片）**：`com.ccz.app.battle` —— 把 game-core 每条命令返回的 `Event` 流投影成表现层瞬态徽章（`BattleEffect`：Damaged/Missed/Defeated），在受影响单位格上飘 `−40`/`Miss`/`KO`。**`effectsOf(events)`** 纯总投影：只 Damaged/Missed/Defeated 成徽章（Moved/TurnEnded 等无），`amount`/`crit`/`combo` 逐一拷自 event **不重算**——表现层持零战斗权威。`BattleUiState.effects` 承载上一条已接受命令的徽章，命中攻击经 `effectsOf(resolution.events)` 设入；新选中 / `clearSelection` / 拒绝均清空（单命令瞬态）。`BattleBoard` 飘字（`EffectBadge`）锚到受影响单位格（死单位 `alive=false` 但保留 tile，`KO` 仍落该格）；顺带把互斥的 `highlighted`/`selected`/`targetable` 三布尔并成单一 `CellMark` enum（满足 detekt 参数上限，视觉等价）。`BattleEffect` 无 Android 依赖 → JVM 单测可测。**门补强**：`BattleEffectTest`（+2：映射保真 + 字段逐拷）+ `BattleReducerTest`（+3：命中徽章金额==权威 hp 降幅、击杀出 KO、重选清旧徽章）；:app 12→17。三镜头对抗审（correctness/architecture-determinism/coverage-lifecycle）**0 发现**。**game-core 零改动**，`Resolver`/RNG/`Formula`/`RULES_VERSION`/golden 零碰。
- **多技能选择 + loadout 权威（P2 渲染半，#37，本片）**：核心决策 ——「单位能用哪些技能」是战斗合法性，归 **game-core 权威**，不归表现层。game-core：`BattleContext.loadouts`（unitId→skillIds）+ `loadoutAllows` 守卫；`CommandValidator.checkAttack` 加 `SKILL_NOT_IN_LOADOUT`；`Gameplay.legalTargets` 加 loadout 过滤（守 query⟺submit parity）+ 新只读查询 `Gameplay.legalSkills`（单位可选攻击技能，loadout∩skills 保序；没配回退全表；非活动/死→空）。**opt-in fail-closed**：配了 loadout 的单位**只能**用其内技能（越界拒），没配的不受约束（沿用全表）= 向后兼容默认，待内容驱动层为所有单位填 loadout 后转全严格（镜像可选 `BattleMap` 对 spawn/move 的接线 dormant，KDoc 诚实标注）。`:app`：`BattleReducer` 选中单位暴露 loadout + 默认首技能，新 `selectSkill` 切技能 → `legalTargets` 重算目标（不在 loadout / 无选中 → no-op），`submitAttack` 用**选中**技能；`BattleScreen` 加 `SkillBar`（FilterChip，>1 技能才显示）；`BattleUiState` 5 选中字段内聚成 `Selection` 子对象、日志格式化抽到 `BattleLog.kt`（过 detekt 内聚/参数门，行为/视觉等价）。`DemoBattle` 配 skill 表 + per-unit loadouts（Guan strike+spear、南 2 格枪兵 → 切技能目标随射程变，首屏可演示）。**门**：`GameplayQueryTest` +3（legalSkills + loadout parity，JVM 211→214）、`BattleReducerTest` 重写用 `Selection` +4（:app 17→21）。三镜头对抗审 **0 发现**。`Resolver`/RNG/`Formula`/`RULES_VERSION`/golden 零碰；save 序列化不碰（loadout 在 `BattleContext` 非 `Combatant`）。
- **内聚 refactor（高内聚低耦合，#39，本片）**：按用户「引擎基础设施内聚是硬要求」落地内聚审计（17-agent 务实 triage）的 do-now，**行为零变化**重构。game-core：新 internal `actorEligibility(state, unitId, active): RejectReason?`（`BattleGeometry.kt`，与 `sameSide`/`occupancyOf` 同类）**单一化** actor 前置（存在→`UNIT_NOT_FOUND`/存活→`UNIT_DEAD`/同侧→`NOT_ACTIVE_FACTION`）——此前在 5 处逐行重复（`CommandValidator.checkMove`/`checkAttack` + `Gameplay.legalDestinations`/`legalTargets`/`legalSkills`）。**一处规则两类消费者共用（validator 报 reason、查询返回空）→ 不会再漂移，`query⟺submit parity` by-construction**。`:app`：新 internal `BattleState.unitAt(pos): Combatant?` 单一化占用读（alive + 同格），替 `tapTile`/`cellAt` 逐字重复（留 `:app`、Android-free，合法性仍归 `Gameplay`）。补两条钉单一化路径的等价/语义测试：`legalSkills(ghost)→空`（JVM 214 不变只加断言）、`tap 死单位格不选中`（:app 21→22）。三镜头对抗审 0 confirmed（2 条 coverage 观察判 pre-existing）。`Resolver`/RNG/`Formula`/`RULES_VERSION`/golden 零碰。
- **app 过场表现层（P2 渲染半 / P3 R dialogue flow，#50 + 本批）**：`com.ccz.app.scenario` —— 把 game-core 确定性 `ScenarioRunner` 的 `Playback` 投影成 Compose 过场（对话/立绘/场景切换/BGM/淡入出 + 选项分支）。**表现层持零剧情权威**——只渲染 `ScenarioRunner.run(script, choices)` 的输出，`choices` 是可回放输入轴（镜像战斗 command 列表），分支/变量/goto/label 全归 `ScenarioRunner`，`:app` 不重算（镜像 §Seven Hard Rules #1/#2 + 战斗侧 `BattleReducer`→`Gameplay`）。核心件：**`ScenarioReducer`**（纯 reducer，零 Android 依赖、JVM 可测；`advance` 推进渲染 cursor、`choose` 追加选项索引重跑，**cursor 保留依赖 runner 的确定性 append-only prefix 契约**——已看过的 op 重跑必逐一复现只追加新 op；**fail-closed**：越界/负索引/未到选择点 `choose` 全 no-op，绝不喂坏索引给 runner）；**`ScenarioUiState`**（派生 `current`/`pausedChoice`/`finished`/`halted`，全读 `playback` 不二次推断，含 budget-halt fail-safe）；**`DemoScenario`** 现为 bundled content RScript 薄访问器，`MainActivity` 串「过场→战斗」流（曹操传式战前剧情）。**game-core 零改动**。门：`ScenarioReducerTest` +11（含直接钉 prefix 不变式的回归测试以锁 cursor-preservation 契约），`:app` 23→34。对抗审双镜头 **0 P1/P2**（correctness：4000-脚本 fuzz 103970 transitions 0 违反，确认 prefix 不变式 + 空/选择优先/budget-halt 各态自洽无死角 + `choose` fail-closed；architecture：零权威泄漏、DAG 干净、`(script,choices)` 确定性、host 生命周期 sound）。`Resolver`/RNG/`Formula`/`RULES_VERSION`/golden 零碰。
- **app 战斗终局（P2 渲染半 / 延迟触发器 ② battle-end 通道，#52，本片）**：让战斗有**可达胜负态**。game-core 新增只读查询 `Gameplay.outcome(state, script): BattleOutcome`（委托既有 `WinLose.evaluate`，纯无 RNG 不改 state），与 `legalDestinations`/`legalTargets`/`legalSkills` **同列一个内聚权威面**（不拆 `GameplayQueries`，遵 leave-alone）；**无新核规则**——复用既有 `WinLose` + `AnnihilateEnemies`/`ProtectAlive` 条件。`:app`：`BattleUiState.outcome` 字段；`BattleReducer` 每条已接受命令后 **poll** `Gameplay.outcome` 刷新终局（自身不判胜负），**终局后 `tapTile`/`selectSkill`/`endTurn` 全冻结**（finished battle 不吃输入）；`BattleScreen` 加胜负横幅；`DemoBattle.script()` 配 `win=[AnnihilateEnemies]`、`lose=[ProtectAlive("guan")]`（主将阵亡=败，**DEFEAT 路径接线但 dormant**——idle 敌不出手，待敌方 AI，KDoc 诚实标注）。**诚实标注**（对抗审 honesty 镜头抓到 P2 已修）：poll 路径不持久化 outcome → `WinLose` 的 state 级 sticky 短路在此 inert，终局黏性实由 `:app` 输入冻结提供（仅 win/lose 条件单调时稳定），记于 `Gameplay.outcome` KDoc。门：`GameplayQueryTest` +5（JVM 214→219）、`BattleReducerTest` +5（:app 34→39）。对抗审双镜头 **0 P1/P2**（correctness 实编译跑 reducer 验输入冻结完整 + 横幅恰发一次 + 无 stale 漏洞；honesty 0 余留）。`Resolver`/RNG/`Formula`/`RULES_VERSION`/golden 零碰。
- **行动经济（Fire-Emblem 式一移一动，#54，本片）**：确立战棋行动经济（联网核实曹操传/英杰传系列为 Fire-Emblem 式 —— 单位移动后仍可行动一次），**敌方 AI 前置**（无经济则 AI 回合循环不终止）。game-core：`Command.Wait`（待机耗尽本回合）+ `Event.Waited`；`BattleProgress.moved`/`acted`（回合作用域，`EndTurn` 清空）；`CommandValidator` 加 `UNIT_ALREADY_MOVED`/`UNIT_ALREADY_ACTED`——规则 = 可移动一次、行动（攻/待）一次、不可二动，**移动后仍可攻**（move-then-attack）；`Resolver` move→markMoved、attack（命中/未命中）→markActed、Wait→markActed、EndTurn→清空；三只读查询 `legal*` 加经济守卫（query⟺submit parity）。**save**：`Command.Wait` 经 `CommandDto`/mappers/`commandIntegrity` 往返；`moved`/`acted` **刻意不持久化**（save 只存全新开局态、replay 折命令重导出，decode 落空默认，无 schema bump），诚实标注为 save-path 假设非结构保证 + encode 处 mid-save 守卫注。`:app`：`BattleReducer` 移动后保留选中使 move-then-attack 在 UI 可达 + `wait()`，`BattleScreen` 加 Wait 按钮，`RejectPhrase` 穷尽 when +2 词条。门：`ActionEconomyTest` +6 + `SaveCodec`/`SaveLoader` Wait 各 +1（JVM 219→227）、`BattleReducerTest` +3 含 move-then-attack/wait/exhaustion（:app 39→42）。对抗审双镜头：correctness 实编译 fuzz 证经济不可破 + 抓到 P1（move-then-attack UI 不可达）已修；save/golden/determinism/exhaustiveness 0 P1，P2 文档过度声称 + P3 缺测已修。`Resolver`/RNG/`Formula`/`RULES_VERSION`/golden 零碰（经济只闸命令受理不碰 resolution）。
- **敌方 AI（战斗可玩闭环，#56，本片）**：战斗终于可玩——此前 End Turn 只翻面、敌方空转，行动经济(#54)落地后 AI 回合可终止。game-core：`EnemyAi.nextCommand(state, context): Command` 纯确定性侵略式规划器——为下一个未耗尽当前侧单位（按 id）：射程内有敌则攻最近（首个能及技能）、否则朝最近敌移动（最小化曼哈顿的可达格）、再否则待机；全员耗尽则 `EndTurn`。纯无 RNG（规划），只在 `Gameplay.legal*` 报的合法集里选、经 `Gameplay.submit` 落子——**零战斗/AI 权威**；靠行动经济终止（每单位至多一移一动）；架构 `:gameplay` 模块拆分待后。`:app`：`BattleReducer.endTurn` 玩家结束后 `runEnemyTurn` 自动驱动敌方（逐条 `nextCommand`→`submit` 直到翻回玩家侧或判定），**中途 DEFEAT 即停**（`ProtectAlive(guan)` dormant 路径转 live——敌方能击杀主将致败），末击徽章前递、逐条日志，`ENEMY_TURN_STEP_CAP` + bail 时 `handBackToPlayer` 强制交还玩家（不滞留敌方侧）。门：`EnemyAiTest` +6（JVM 227→233）、`BattleReducerTest` +1（:app 42→43，改写 endTurn 翻面测为整回合 +2）。对抗审双镜头：planner 0 发现（400-脚本 fuzz 证终止 + 确定 + 不产非法命令）；integration 0 P1，P2（bail 滞留敌方侧）+ P3（测补交还断言）已修。`Resolver`/RNG/`Formula`/`RULES_VERSION`/golden 零碰（AI 只在 reducer 之上，不在 replay/golden 路径）。
- **小票夹金矿移植（#41 + #42 + #44 + #46 + #48，本批）**：开采 fork 来源 `xiaopiaojia`（成熟记账后端）里可移植到 CCZ 的工程实践（5-lens × 28-agent triage workflow，23 候选分四档；可推导半移植、域专属跳过）。完整清单见 `docs/audits/2026-06-22-xiaopiaojia-port-backlog.md`。**已移植**：① **高内聚低耦合总纲**（`GENERAL §Module Boundaries`，fork 时**静默丢失**——grep 确认 CCZ docs 此前零「内聚/耦合」字样——捡回，`[review-only]`）；② **注释纪律**（`GENERAL §Comment Discipline`，WHY 不复述 WHAT，落地用户「注释克制」指令）；③ **baseline 叠债 tripwire**（超阈值 detekt 冻结单元不得加新功能）——以上 #41，规则 v0.1.0→v0.2.0；④ **`RejectReason→phrase` 层**（`:app RejectPhrase.kt`，穷尽 when fail-closed，内部 token 不泄漏给玩家，**比 `errors.py` dict+运行时 fallback 更强**：编译期强制全覆盖）；⑤ **detekt `ignoreSingleWhenExpression`**（穷尽映射 when 不被圈复杂度误伤，门细化非放松）——以上 #42；⑥ **依赖方向门 `assertModuleDependencyDirection`**（根 gradle，模块集从 `settings.gradle.kts` 驱动 fail-closed，强制 `native-content/save-io/app → game-core` 单向 DAG）——**把高内聚低耦合总纲从 `[review-only]` 升 `[machine-gated]`**，兑现「规则要么有门要么诚实标注」元规则（"标杆 = 规则有牙"）；对抗审抓到初版 over-claim fail-closed（只扫硬编码模块、不读 settings）**已修** + 诚实标注覆盖边界——#44；⑦ **文本编码门 `verifyTextEncoding`**（根 gradle，扫 `git ls-files` 跟踪文件，强制 UTF-8 + `.ps1` BOM + 无 U+FFFD 乱码）——`§Windows/PowerShell Rules` 升 `[machine-gated]`；对抗审抓到 3 confirmed（fail-open 空扫描 / 非 ASCII 路径八进制转义致漏扫 / `.ps1` 大小写不一致）**全修** + fail-closed 加固（git exit code + `checked>0` + `-z`/`quotepath=false`）——#46；⑧ **依赖版本审计门 `assertStableDependencyVersions`**（根 gradle，扫所有构建脚本 root+settings+各 settings 登记模块 `build.gradle.kts`〔模块集从 settings 读 fail-closed〕+ wrapper `distributionUrl` 的版本字面量〔plugin-DSL / 命名参 `version=` / detekt `toolVersion` / Maven 坐标 / gradle 发行版〕，非纯数字点分且末段非 `RELEASE/FINAL/GA` 即判预发布 fail，除非精确版本串在 `adrSanctioned` 锁步 ADR-0003 detekt alpha 例外）——把 `§Dependency Governance` 禁预发布条升 `[machine-gated]`（规则 v0.2.0→v0.2.1），停止维护/来源不清仍 `[review-only]`（机器判不了）；对抗审（双镜头）抓到 2 confirmed **已修**：① `isStable` 用 `contains` 子串判 RELEASE/FINAL/GA 致 fail-open（`2.0.0-omega` 含 GA、`1.0.0.RELEASE-SNAPSHOT` 含 RELEASE 误判稳定）→ 改末段锚定；② 命名参 `version = "x"` 漏扫 → 补 `namedArgVersionRe`；覆盖边界（version catalog / 动态版本 / BOM 传递 / `val` 间接引用 / `id().version()`）诚实标注于门注释——#48。每片过三/双镜头对抗审（#44/#46/#48 各抓到 confirmed 缺陷已修——对抗审在三个 gradle 门上连续抓到 fail-open 类问题，证明其有效；余片 0 confirmed）。`Resolver`/RNG/`Formula`/`RULES_VERSION`/golden 零碰。**仍待移植**（见 backlog）：per-slice report、~~`KNOWN_ISSUES.md` ledger~~ **已落地（#60，`docs/KNOWN_ISSUES.md`——dormant/defer 项的单一分级真相源，后续读它而非挖此 run-on）**、~~rollback runbook~~ **已落地（#61，`BACKUP_RESTORE.md` 编号回滚步骤 + save-schema 风险边界）**、~~ship-slice 三对账步~~ **已落地（#61，SKILL step 8）**、~~per-slice report / run-on~~ **已落地（#62，line 3 terse 化）**、~~`BRANCH_BASELINE.md`~~ **已落地（#62）**。**移植 backlog「待移植」#1–#8 全数清空**（机器门 #41–#48 + 工程文档 #60/#61/#62）；余下仅 `等阶段（defer，带触发器）` 项（依赖审计脚本 / `REFERENCES.md` / `RELEASE_PACKAGING.md` / 真机验收 runbook / PR-chain ledger，触发器未到）。
- **内容驱动战役驱动层（战斗 + intro 过场闭环，#63 + 本批）**：战斗不再硬编码——`CampaignAssembler`（native-content）把已验证 `NativeContent` 包装配成 `BattleSetup`（`BattleContext` + 经 `TriggerRunner.applyPre` 部署的 `BattleState` + 入口 `SScript`）：`MapDef`+terrain→`BattleMap`（新增 `TerrainDef.passable`，未用 bonus 收进 `TerrainBonuses` 子对象避 detekt 6-参）、`ClassDef`→`UnitClass`、`SkillDef`→`Skill`、loadout、`SScript.pre` 的 SpawnUnit 部署（map 接进 `ScriptContext` 使越界/墙 placement 拒转 **live**）；fail-closed（缺 script/map/terrain id、部署被拒 → `CampaignAssemblyException`，不产半成品）。**DAG 决策（ADR-0005）**：`:app` 作组合根，加 `:app → :native-content` 边（`app → native-content → game-core` 无环，依赖方向门 `allowed` 同步），不建新 `:campaign` 模块（YAGNI）。`CampaignContent`（app）= demo 内容包，`DemoBattle` 由硬编码 core-type 种子**重写为 `CampaignAssembler` 薄访问器**（既有 `:app` reducer 测试转为对装配产物的端到端覆盖，零 assertion 改 = 强证装配复现 demo）。对抗审 3 镜头（correctness/fail-closed/coverage）0 P1、3 P2 已处理（补 MoveRejected 部署拒测、非方形 4×3 map 钉 x/y 朝向、`pre` 段 Move/Remove 缺单位静默 no-op 诚实标注；本批改为 `pre` 部署顺序 guard 并移出 KNOWN_ISSUES）。**JSON 资源装载已落地**（PR3）：`CampaignContent` 改从 `app/src/main/resources/content/ccz_demo/campaign.json` 经 `ContentJsonLoader` 解码（真实管线端到端，资源打进 APK 已验证；reducer 测试零改即过 = JSON 解码出等价战斗），content 现以**数据**存在。**intro 过场内容化已落地**：`events.portrait_subjects` 显式声明非战斗 portrait 主体，`demo_intro` RScript 进入同一 `campaign.json`，`DemoScenario` 改为 `CampaignContent.introScript()` 薄访问器；未知 portrait / 与 unit id 碰撞 fail-closed。**mid-trigger tick 已落地**（PR4）：`BattleSetup` 加 `scriptContext`（reserves+map，与 `BattleContext.map` 同实例），`BattleReducer` 每条已受理命令后跑 `TriggerRunner.tick` → `SScript.mid` 触发器真正 fire（`once` 跟踪）+ win/lose settle；verdict 改读 settle 后 `state.outcome`（trigger-driven `force_win/lose` 也 surface，非仅条件 poll）；`tick` 仍归 game-core，reducer 只转发。亦使 mid-battle 脚本 spawn/move 的越界/墙拒转 live（map 同实例线程进 tick）。test baseline JVM 234→252、`:app` 43→54。对抗审 3 镜头（outcome-path/tick-placement/boundary）1 P2（trigger 结构事件未上表现层，demo dormant 故诚实标注+记 KNOWN_ISSUES）、余 0。**replay 接线已落地**（本批）：新增 `CampaignReplayDriver`（`:app` 组合层），把 bundled content 的 `BattleContext` class/skill 表与 R-script 表传给 `SaveLoader`/`ScenarioReplayer`；成功重放、未知 script、choice 不完整均有 app 单测。`Resolver`/RNG/`Formula`/`RULES_VERSION`/golden 零碰（tick/intro 装载纯无 RNG，不在 replay/golden 路径）。
- 确定性铁律守住：`Resolver`/RNG 消费顺序/伤害公式/`RULES_VERSION`(=1)/golden 全程未动；每片过 2-3 镜头对抗审 0 P1。
- 主线 Android-first：`game-core`（纯 Kotlin/JVM 唯一战斗权威）+ `native-content`（内容模型 + validator + JSON loader）+ `save-io`（存档原子写 IO 边界，纯 JVM）+ `:app`（Compose 表现层，渲染战场 + tap→command 经 `Gameplay`，只渲染、不持第二套真相）。
- 本地全量门 = `docs/runbook/LOCAL_DEV.md` 的 Full Current Local Gate（从 `android/` 内跑）。
- 仓库自 Godot tactical RPG 模板 fork，上游模板素材已**彻底移除**（含 git 历史）。
- 规则体系已重锚到 CCZ 域并接机器门（PR #1 / #2，详见 `docs/audits/2026-06-20-rules-overhaul-vs-xiaopiaojia.md`）；`[machine-gated]/[review-only]/[aspirational]` 状态标随每片诚实翻转（不给未实现代码写现在时断言）。本轮把命令合法性、win/lose、触发器+ops、JSON 解码边界、Save 拒未来版本从 review-only/aspirational 翻成 machine-gated。

## 在途任务

- 无（成长/品质评级 + 真实战斗进 app 弧线 #82–#93 + 自主优化审计扫荡 #95–#98 + 优化 backlog 三片〔地形检视 #8 / 敌方 AI 低血优先 / LegacyBattleBuilder DRY #9/#10〕收尾,工作树干净）。优化审计高价值项已扫净;余项审计判低价值/高风险或需独立 ADR,已诚实 defer（见下方 bullet）。

## 下一步（V1 Roadmap，每片走 `skills/ship-slice`）

- ~~事件脚本 JSON 解码（P1 续）~~ **已落地**（多态 op + class-discriminator 白名单 + fail-closed，`EventTables` 从 JSON 装载）。
- ~~从 `UnitDef` 装配 `ScriptContext.reserves`（SpawnUnit 运行期模板）~~ **已落地**（本片：`BattleAssembler` 装配 off-map 满血模板，spawn op 真正取到 reserve）。~~剩余：`SpawnUnit` 的 `at` 目标格暂不校验边界/占用~~ **已落地**（本片：game-core 运行期 `BattleOps.spawn`→`obstruction()` fail-closed —— 占用恒查、边界/可通行经可选 `ScriptContext.map`，缺/越/墙/占 → `Event.SpawnRejected`）。~~把 `BattleMap` 接进生产 spawn 路径~~ **已落地**（`CampaignAssembler` 装配同一个 `BattleMap` 给 `BattleContext`/`ScriptContext`，spawn/move 越界/墙拒转 live）。~~内容侧脚本落点上界校验~~ **已落地**（本批：装配已选 `SScript + MapDef` 前预扫脚本坐标，越界 fail-closed）。
- ~~R 剧本 scenario 流执行（scenario runner）~~ **已落地**（`ScenarioRunner` 确定性 PC 执行 + 控制流消化 + 演出 op 产出 + 循环预算防护）。~~`Choice` 玩家选择 = 可回放输入~~ **已落地**（本片：`run(.., choices)` 用选择索引序列驱动 Choice，应用 setVars + goto，耗尽/越界 fail-safe 暂停；R 剧本 `(script,vars,choices)` 确定回放）。~~把 scenario choices 序列接进 `SaveEnvelope` 存档~~ **已落地**（`SaveEnvelope.scenarios`，save schema 1→2，v1 向后兼容）。~~scenario 回放**执行** + 完整性~~ **已落地**（本片：`ScenarioReplayer` 对 `SaveEnvelope.scenarios` 跑 `ScenarioRunner`，scriptId/choices fail-closed 完整性）。**R 剧本 scenario 流端到端闭环**（执行→选择→存档→回放）。~~把 `SaveLoader`（战斗）+ `ScenarioReplayer`（过场）+ content 接进真实战役驱动层（`:app` / 组装层）~~ **已落地**（`CampaignReplayDriver` 传 content script 表，战斗轴 + scenario 轴一起 fail-closed）。
- ~~Save on-disk codec~~ **已落地**（本片：`SaveCodec` `SaveEnvelope`↔JSON，shape + enum fail-closed，round-trip 全字段保真）。~~命令完整性优雅拒绝~~ **已落地**（`SaveLoader.commandIntegrity`：信封命令引用缺失 unit/skill → `CORRUPT_COMMAND` 优雅拒，replay 前）。~~剩余：§Write & Convert Safety 原子写机器门~~ **已落地**（本片：`:save-io` 的 `SaveFileStore` 临时文件 + `ATOMIC_MOVE`，fail-closed，存档原子写翻 `[machine-gated]`）。剩余：converter 幂等批处理（IO 层）仍 `[aspirational]`，待 converter 模块入仓（等真实 MOD 样本）。（scenario choices 进存档已落地，见上。）
- **团队 fail-closed 扫荡的后续**（本轮 8 片已收 13 个 gap 的主体，剩低风险/需上层的尾巴）：① ~~S7 driver caveat~~ **已落地**：`CampaignReplayDriver` 必传 content script 表给 `SaveLoader`，未知 script → `CORRUPT_SCENARIO`；② ~~scenario `INCOMPLETE_REPLAY` load parity~~ **已落地**：同驱动重跑 `ScenarioReplayer`，choice 不完整 → `INCOMPLETE_REPLAY`；③ ~~`manifest.entry` 存在性校验~~ **已落地**：入口必须指向已知 `events.sScripts` 或 `events.rScripts` id；④ ~~事件脚本落点上界校验~~ **已落地**：`CampaignAssembler` 在已选 `SScript + MapDef` 上预扫 `pre/post`、`mid` reach/action、`win/lose ReachTile` 坐标，越界即 `CampaignAssemblyException`。
- P2 渲染半：~~建 `:app` 壳~~ **已落地**（#31）。~~渲染 map/units + 输入→command（经 `Gameplay.submit`，**不得**绕核改结果）~~ **已落地**（#32：Compose `BattleBoard`/`BattleScreen` 渲染网格 + 单位 + 可达高亮 + 日志；tap→`Move`/`EndTurn` 经 `BattleReducer`→`Gameplay`；`Gameplay.legalDestinations` 只读高亮查询；`:app` 单测真跑门）。~~攻击目标选择 UX（tap→`Attack`，目标拣选 + 射程高亮，复用 `CommandValidator` 攻击合法性）~~ **已落地**（#33：`Gameplay.legalTargets` 只读查询 + `BattleReducer` 攻击路由 + 可攻目标红色高亮 + stale target fail-closed，表现层零战斗权威）。~~事件驱动表现层（消费 `Event` 流演出 Damaged/Missed/Died）~~ **已落地**（#35：`effectsOf` 把权威 `Event` 投影成飘字徽章 Damaged/Missed/Defeated，表现层零战斗权威；Scenario 演出已由 #50 与 content/replay 接线覆盖）。~~多技能选择 UX（per-unit loadout / skill picker）~~ **已落地**（#37：core 单位技能 loadout 权威 + `Gameplay.legalSkills` + `SKILL_NOT_IN_LOADOUT` 校验 + `:app` `SkillBar`，射程随技能变；loadout opt-in fail-closed，没配回退全表待驱动层填全）。~~事件驱动 Scenario 演出（过场 op 进表现层）~~ **已落地**（#50：`scenario.ScenarioReducer`/`ScenarioScreen` 投影 `ScenarioRunner` 的 Playback 成 Compose 过场，choices 可回放输入轴，表现层零剧情权威，`MainActivity` 串过场→战斗；见上「app 过场表现层」）。~~剩余：接 content 进真实战役驱动层（亦解 spawn/move 的 `BattleMap` 生产接线 dormant 缺口；`DemoBattle`/`DemoScenario` 硬编码种子届时由内容装载替换）~~ **战斗 + intro 过场已落地**（#63 + 本批：`CampaignAssembler` + `:app→:native-content` 组合根 + `CampaignContent` demo 包，`DemoBattle` 转内容装配薄访问器，`DemoScenario` 转 `CampaignContent.introScript()`，spawn/move `BattleMap` 部署接线转 live；见上「内容驱动战役驱动层」）。~~`SaveLoader`（战斗回放）/`ScenarioReplayer`（过场回放）replay 接线~~ **已落地**（`CampaignReplayDriver`）。`:app` 仍待补的未来门（`[aspirational]`，见 `CCZ_ENGINE_RULES.md` §Android App Gates）：Room schema drift、R8 release 编译、apksigner 指纹钉、emulator smoke（AVD `ticketbox_api36_host`）。
- **成长 / 品质评级 + 真实数据导入 + 真实战斗进 app（ADR 0006，#82–#93）**：「导入期成长预算（Budgeted Growth）」骨干已落地 —— 成长是创建/导入期的纯确定性函数，折算成整数面板快照写进 `initialState`，game-core 永不重算。分片：逐级成长预算 + 默认惰性骨架（#83）、真实 `dic_job` 成长率喂进引擎（#84）、引擎自有 6 档品质杠杆 `100/120/…/200`（#85，非复刻 `dic_grade`）、demo 老兵让杠杆在真实内容可见（#86）、从真实 `dic_hero` 战力开矿锻品质档 0..5（#87，**旧作当矿、新引擎当刀**，阈值标定 2729 武将分布、不复刻 `dic_grade` 档名/概率波动）、**出场等级 capstone（#89）** —— `LegacyBattleBuilder.Placement.level` 让合成战斗按战役元数据指定出场等级>1 部署真实武将，整条链（真实战力→品质 + 真实成长 × 出场等级）首次**咬合见效**（关羽 lv10 = atk 163/hp 326，端到端证明；同武将 lv1 = base 恒等，证明等级是开关非改数据）。全程**回放安全**：导入期纯整数预算、零新增 RNG、golden 走默认内联路径不触发、`RULES_VERSION` 保持 1。**真实数据进可玩 app（#91–#93）**：`LegacyPackGenerator`（离线 converter,gradle `:mod-import:generateLegacyBattle`,非门）从真实解密表生成真实战斗包 `ccz_daxingshan`(大兴山:刘备/关羽/张飞 vs 程远志/邓茂,真实属性+成长+战力派生品质档+出场等级);`RealBattle` 装配、`MainActivity` 接它(#91);战斗上**真实地形**(terrainMap_1 裁 8×7,真实 `dic_jobWalk` 移动力 + `dic_jobTerrain` 系数,#92);**真过场** `RealScenario`(桃园三兄弟 大兴山 战前,#93)。版权非阻碍(自有同人数据,用户决定);`verifyTextEncoding` 补扫 `.json`。**剩余(需更大工程,非清扫级)**:① **真战法效果**——大部分 `dic_seid`/`dic_skill` 是治疗/buff/debuff/范围/天气(`hurt=0`),引擎只建模伤害技能,故真技能效果需引擎技能效果系统(**ADR 0008 Accepted,Phase 1 已落地**:`Command.Cast` + `Resolver.cast` 确定性单目标治疗〔`SkillEffect.Heal`,零 RNG、`GoldenReplayTest` 逐字节不变、不 bump RULES_VERSION;save schema v2→v3 仅为新命令变体〕,sealed `SkillEffect` 端态就位。**已接进可玩 app**:`Gameplay.legalCastTargets`(SELF/ALLY 带,与 checkCast 经 `castTargetAllows` 单源化 parity)+ `BattleReducer` cast 路由(选 effect 技能→castSkill,tap 友军 → `Command.Cast`)+ 棋盘绿色 cast 高亮 + 治疗 `+N` 徽章;大兴山刘备(hero_1)获治疗战法 `skill_2`「疗伤」(range 0-1,ALLY,heal 60),玩家可对自身/邻接友军施放。剩 Phase 2+:瞬时 stat-delta/cleanse → duration/status(持久态 schema bump)→ 概率/RNG(RULES_VERSION bump)→ AoE/召唤/控制/天气/被动 aura;skill_2 系手添进生成内容,待 importer 片让转换器真出 heal(见 KNOWN_ISSUES),见 `docs/DECISIONS/0008-skill-effects-system.md`)。② 全图滚动(`BattleBoard` 固定 44dp 格不滚动,故地图裁剪)。③ 完整**转生制** + 旧作随机升/降档概率波动(独立 ADR,见 0006 第三阶段 + Rollback)。细节见 `docs/DECISIONS/0006` 与 MEMORY `bgt1-resource-key`。
- **真实数据接进引擎（#67–#81，详见 MEMORY `bgt1-resource-key`）**：BGT1 资源解密 + 旧作表导入器（#67–#71）、地形战术全套（移动/感知 AI/战斗系数/防御/闪避/回血，#72–#81）均已落地，全部默认惰性、map 经 `ResolveContext` 从内容穿入不入存档，故回放/golden 确定性守住、`RULES_VERSION` 不动。
- **自主优化审计扫荡(#95–#98)**:4 镜头审计(内聚/简化·性能·健壮性·游戏质量)+ 对抗验证 → 13 项(5 high),已执行高价值 + 廉价 med 项:① fail-closed 补 `terrain_cost` 未知键 + 单位 `level>=1`(#95);② 棋盘双向滚动 + 每格 O(1) 预算渲染 + 单位 hp/上限着色(#96);③ HUD 显示胜负目标(#97);④ 真实地形守方加成(防/避/回血)进真实战斗,地形掩护转 live(#98)。全程回放安全(内容轴/纯表现层/load 校验,golden/RULES_VERSION 零碰)。**剩余 backlog**(审计已记,价值/工作量见 PR 审计):~~地形格检视 UI(#8)~~ **已落地**(本片:tap 任意格 → `BattleUiState.inspected` 记录 + `TerrainPanel` 显示地形名/移动消耗/防·避·回血掩护/占据者,把 #98 已 live 但**不可见**的地形加成翻给玩家;`terrainInfoAt`/`terrainBonusLines` 纯函数 JVM 可测,`BattleLabels` 收口 skill+terrain 名查找,5 参守 detekt;native-content `TerrainBonuses` stale 注释同修;纯表现层 game-core 零碰,3 镜头对抗审 0 发现)、~~`LegacyBattleBuilder` DRY(#9/#10)~~ **已落地**(本片:抽 `battleScript`〔win/lose + 越界校验 spawn 映射,#10〕+ `assemble`〔base 包装配,#9〕单源化两 builder,行为零变 byte-identical;`:mod-import:test` 全过 + 2 镜头对抗审 0 发现;**注:CI 不跑 mod-import test/detekt,靠本地门 + review 守**)、~~敌方 AI 启发式精修(低血优先)~~ **目标低血优先已落地**(本片:`EnemyAi.attackCommand` 改为 focus-fire 最残血可及敌〔tie 最近→id〕,集火最弱〔HP 代理非预测击杀〕;纯无 RNG、零权威、不在 golden/replay 路径;等血回退最近/id 故旧测不破)、剩 **本方单位行动序**(低血单位先动/兵种序,需 class 速度建模,KNOWN_ISSUES 已记)。**余两项审计判低价值/高风险,按 ADR 0006 Hard Rule #7 诚实 defer(记下不 churn)**:`Resolver.DEMO_SKILLS` 移出生产默认(default 参跨广 call-site + 疑触 golden 默认路径,收益小,待内容驱动层为全单位填 skills 后再做)、`legalSkills` 回退分支 `filter { it in skills }` 微赘(仅 loadout 分支真需,值不抵改测风险)。
- 仍 pending：converter 模块 opcode fail-closed（等真实 MOD 样本，不猜）；`Hexzmap.e5` 二进制大地图（低价值，缓做）。
- **内聚/耦合未来触发器**（高内聚低耦合评估的 5 条 defer，记**阈值**以免过早重构；do-now 已在 #39 收）：① **`RejectReason` 措辞层**：当 UI 不经 query 预过滤直接 submit、或加「为何非法」反馈时 → 在 `:app` 加穷尽 `phrase(reason)`（无 else，新 reason 不编译即提醒）；② **battle-end / 敌方回合通道**：~~battle-end 通道~~ **已落地（#52）**——`:app` 经只读 `Gameplay.outcome`（poll `WinLose.evaluate`，非 settle）surface 终局到 `BattleUiState.outcome`，终局冻结输入；player 命令现可达 VICTORY。~~敌方 AI~~ **已落地（#56）**——其前置「行动经济」#54，AI 本体 `EnemyAi` + `:app` 自动敌方回合 #56；触发器 ② 整条闭环（player 命令可达 VICTORY、敌方 AI 可达 DEFEAT，`ProtectAlive(guan)` dormant 转 live）。**剩余**：AI 侵略式（远程 kiting 已落地 #58——趋向可攻位；HP/兵种行动优先级等启发式仍是后续精修）；架构 `:gameplay` 模块（battle loop / AI / trigger runner）正式拆分仍待（现 `EnemyAi`/`Gameplay`/`WinLose`/`TriggerRunner` 暂居 game-core）；③ **loadout 默认转 fail-closed**：当内容驱动层保证为每个单位填 loadout 时 → 同时删 `legalSkills` 的 `?: skills.keys.toList()` 与 `loadoutAllows` 的 `?: return true`（缺 loadout = 无攻击技能，fail-closed），**不要提前单做**；④ **`tapTile` 意图编码**：当 `Command` 加 Pos-targeted/AoE 变体 或要 move-then-act 时 → 路由加显式 mode 维度；⑤ **effects 逐事件时序**：当需逐击动画 / in-battle cutscene（即「事件驱动 Scenario 演出」片）时 → effects 从整列表投影 + 每命令清空 转带时序播放。评估的 leave-alone：`submit` + `legal*` 是一个内聚权威面（**别拆** `GameplayQueries`）、`Selection`/`BattleLog` 抽出是真设计、`legal*` 是薄但正确的 preview 层。
- 纪律：改 Kotlin 走 `android-detekt-discipline`；搬/删/改签名走 `safe-code-change`；出码后无外部评审走 `adversarial-review`；CI 红走 `ci-red-triage`；**改 `game-core` 把高内聚低耦合当一等门**（引擎基础设施，核心规则重复现在就单一化，见上「内聚 refactor」）。
